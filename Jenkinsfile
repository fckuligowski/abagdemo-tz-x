pipeline {
    agent { dockerfile true }
    stages {
        stage('Test') {
            steps {
                sh 'echo try it again and again and again'
                sh 'ls -al'
            }
        }
    }
}