pipeline {
    agent { 
        dockerfile true 
    }
    stages {
        stage('Test') {
            steps {
                sh 'echo again fn hard'
                sh 'ls -al'
            }
        }
    }
}