pipeline {
    agent { dockerfile true }
    stages {
        stage('Test') {
            steps {
                sh 'echo when will they just tell you how to set this up'
                sh 'ls -al'
            }
        }
    }
}